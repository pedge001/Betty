{
  "name": "betty-hangar24-ops",
  "version": "1.0.0",
  "private": true,
  "description": "Front-end prototype of Betty — Hangar 24 Ops (static site)",
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
